<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Login</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="/public/style.css" rel="stylesheet">
</head>

<body class="d-flex flex-column min-vh-100">

<main class="flex-grow-1 d-flex align-items-center justify-content-center">

    <div class="dashboard-container shadow p-4 bg-light rounded" style="background-image: linear-gradient({{color}}, white);">
        {{#pregunta}}
            <div class="question-container text-center">
                <div class="mb-2">
                    <img src="../public/categorias/{{categoria}}.png" alt="{{categoria}}" width="70" height="70">
                </div>
                <h2 class="mb-4">{{pregunta}}</h2>
                <form action="../partida/validarRespuesta" method="POST">
                    <div class="row mb-4">
                        <div class="col-6">
                            <button type="submit" name="opcion" class="btn btn-primary w-100 mb-2" value="1" {{#respuestaDada}}disabled{{/respuestaDada}}>{{opcion_1}}</button>
                        </div>
                        <div class="col-6">
                            <button type="submit" name="opcion" class="btn btn-primary w-100 mb-2" value="2" {{#respuestaDada}}disabled{{/respuestaDada}}>{{opcion_2}}</button>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-6">
                            <button type="submit" name="opcion" class="btn btn-primary w-100 mb-2" value="3" {{#respuestaDada}}disabled{{/respuestaDada}}>{{opcion_3}}</button>
                        </div>
                        <div class="col-6">
                            <button type="submit" name="opcion" class="btn btn-primary w-100 mb-2" value="4" {{#respuestaDada}}disabled{{/respuestaDada}}>{{opcion_4}}</button>
                        </div>
                    </div>
                </form>
            </div>
        {{/pregunta}}

        {{#respuestaDada}}
            <div class="result-container mt-4">
                {{#correcta}}
                    <div class="alert alert-success" role="alert">¡Felicidades, acertaste!</div>
                    <div class="alert alert-info" role="alert">+1 Punto</div>
                    <div class="mt-4 text-center">
                        <div class="row">
                            <div class="col-6 mb-2">
                                <!-- Botón para abrir el modal de reporte -->
                                <button type="button" class="btn btn-danger w-100" data-bs-toggle="modal" data-bs-target="#reportarModal">Reportar</button>
                            </div>
                            <div class="col-6 mb-2">
                                <button type="button" class="btn btn-success w-100" onclick="window.location.href='../partida/traerPregunta'">Siguiente pregunta</button>
                            </div>
                        </div>
                    </div>
                {{/correcta}}
                {{^correcta}}
                    <div class="alert alert-danger" role="alert">Perdiste, intenta de nuevo</div>
                    <div class="alert alert-info" role="alert">Tu puntaje final es: {{puntaje}}</div>
                    <div class="mt-4 text-center">
                        <div class="row">
                            <div class="col-6 mb-2">
                                <!-- Botón para abrir el modal de reporte -->
                                <button type="button" class="btn btn-danger w-100" data-bs-toggle="modal" data-bs-target="#reportarModal">Reportar</button>
                            </div>
                            <div class="col-6 mb-2">
                                <button type="button" class="btn btn-success w-100" onclick="window.location.href='../usuario/home'">Al home</button>
                            </div>
                        </div>
                    </div>
                {{/correcta}}
            </div>
        {{/respuestaDada}}

    </div>

</main>

<!-- Modal para reportar pregunta -->
<div class="modal fade" id="reportarModal" tabindex="-1" aria-labelledby="reportarModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="reportarModalLabel">Reportar pregunta</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form action="../partida/reportarPregunta" method="POST">
                <div class="modal-body">
                    <input type="hidden" name="id_pregunta" value="{{pregunta.id}}">
                    <div class="mb-3">
                        <label for="motivo" class="form-label">Motivo del reporte</label>
                        <textarea name="motivo" class="form-control" id="motivo" rows="3" required></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                    <button type="submit" class="btn btn-danger">Enviar reporte</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
